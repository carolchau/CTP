{{<layout}}
  {{$content}}

    <h1>{{ title }}</h1>
    <p>Welcome to {{ title }}</p>
    <p> Your username is: {{user.email}}</p>
    <p> Change password:
      <input type="password" name="changePassword" placeholder="" required>
      <button class="btn btn-primary" type="submit">Change</button>
    </p>

    <h1> Your Reviews: </h1>
    <!-- Load Reviews -->
    {{/content}}
{{/layout}}
